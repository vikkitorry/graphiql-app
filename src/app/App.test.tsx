import { expect, vi, test, describe, beforeEach } from 'vitest';

describe('App', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  test('renders error page', () => {
    expect(1).toBe(1);
  });
});
